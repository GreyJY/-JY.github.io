//basic版本
public static void int f(int n){
if(n==0)return 0;
if(n==1)return 1;

int x=f(n-1);
int y=f(n-2);
return x+y;

}

//优化版本(数组记录记忆查表法)
//用于初始化记忆数组
public static int fibonacci(int n){
int [] cache =new int [n+1];//这种元素数量写法是为了把元素位置放到对应数字索引位置
Arrays.fill(cache,-1);//先把数组里面所有元素初始化成为-1;
cache[0]=0;//初始化是0
cache[1]=1;//初始化是1
return f(n,cahe);//返回计算方法
}
private static int f(int n,int []cache){
if(cache[n]!=-1){
return cache[n]; //这个相当于寻找是否有现存数据，有就直接返回 
}
int x=f(n-1,cache);
int y=f(n-2,cache);
cache[n]=x+y;//这个就相当于第几个数字就是x和y的和
return cache[n];//然后存下这个数组新的元素，方便后续直接数组去用
}
