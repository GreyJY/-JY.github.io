class DSU{
    int[] p;
  public   DSU(){
        p=new int[n+1];
        for (int i=1;i<n;i++)p[i]=i;
    }
    int find(int x){
        return p[x]==x?x:(p[x]=find(p[x]));
    }
    void union(int a,int b){
        p[find(a)]=find(b);}

}

