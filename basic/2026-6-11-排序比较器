//Lambad表达式exmple:按分数排序学生降序
class Student{
String name;
int score;
Student(String name,int score){
this.name=name;
this.score=score;
}
//重写方法注释可以重构下面再调用就是你重写的方法
@Override
public String toString(){
return name+":"+score;
}
}

public class Main{
public static void main(String []args){
Student []studentsArray={
new Student("Alice",85);
new Student("Bob",92);
new Student("Charlie",78)
};
//排序的第一种写法
Arrays.sort(studentsArray,(s1,s2)->s2.score-s1.score)
sout(Arrays.toString(studentsArry));
//排序的第二种写法
List<Student>students=Arrays.asList(studentArry);
sout(students)
//排序第三种写法自己定义sort
Comparator<Student>descByScore=(s1,s2)->s2.score-s1.score;
Comparator<Student>descByScore2=Compartor.comparingInt((Student s)->s.score).reversed();//方法重叠
//排序第四种写法
Comparator<String>descByLength=(s1,s2)->s2.length()-s1.length();
//求第K大方法
@Test
PriorityQueue<Integer>minHeap=new PriorityQueue<>(可以写方法,容器);
for(int num:nums){
minHeap.offer(num);
if(minHeap.size()>k){
minHeap.poll();//移除最小的
}
}
return minHeap.peek();//优先队列中的排头
}
int []nums={3,2,1,5,6,4};
int k=2;
sout(方法)
}
